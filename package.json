{
  "dependencies": {
    "@radix-ui/react-popover": "^1.1.14"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "start": "concurrently \"npm --prefix back-end run cdev\" \"npm --prefix front-end run dev\" \"npm --prefix manage-end run start\""
  }
}
